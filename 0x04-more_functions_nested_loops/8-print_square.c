#include "main.h"
/**
 * print_squares - print a square followed by a new line
 * @size: The number of squares
 * Return: empty
 */

void print_square(int size)
{
	int x, y;

	if (size <= 0)
	{
		_putchar('\n');
	}
	else
	{
		for (x = 0; x < size, x++)
		{
			for (y = o; y < size; y++)
			{
				_putchar(#);
			}
			_putchar('\n');
		}
	}
}
